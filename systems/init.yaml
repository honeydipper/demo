systems:
  matrix:
    extends:
      - kubernetes
    data:
      kubernetes:
        source:
          type: local

  job_runner:
    extends:
      - matrix
    data:
      git_url: git@github.com:honeydipper/infrastructure_demo.git
      git_key_secret: git-deploy-key
